cmake_minimum_required(VERSION 3.25)
project(DSPAASR VERSION 1.1.0 LANGUAGES CXX)

if(NOT WIN32 OR NOT MSVC OR NOT CMAKE_SIZEOF_VOID_P EQUAL 8)
    message(FATAL_ERROR "DSPAAMod currently requires Windows x64 and MSVC.")
endif()
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
set(NGX_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/external/ngx" CACHE PATH "Official NVIDIA DLSS SDK")
if(NOT EXISTS "${NGX_ROOT}/include/nvsdk_ngx.h" OR NOT EXISTS "${NGX_ROOT}/lib/nvsdk_ngx_d.lib")
    message(FATAL_ERROR "NGX SDK is missing. Run tools/fetch-ngx.ps1 first.")
endif()
add_library(ngx-sdk STATIC IMPORTED)
set_target_properties(ngx-sdk PROPERTIES
    IMPORTED_LOCATION "${NGX_ROOT}/lib/nvsdk_ngx_d.lib"
    INTERFACE_INCLUDE_DIRECTORIES "${NGX_ROOT}/include")

add_library(dspaa-core STATIC native/core/preset-evidence.cpp)
target_include_directories(dspaa-core PUBLIC native)
target_compile_options(dspaa-core PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET dspaa-core PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_library(dspaa-ngx STATIC native/ngx/session.cpp)
target_include_directories(dspaa-ngx PUBLIC native)
target_link_libraries(dspaa-ngx PUBLIC ngx-sdk d3d11)
target_compile_definitions(dspaa-ngx PUBLIC NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
target_compile_options(dspaa-ngx PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET dspaa-ngx PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

set(FSR_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/external/fsr-sdk/Kits/FidelityFX" CACHE PATH "Official AMD FSR SDK 2.3")
if(NOT EXISTS "${FSR_ROOT}/upscalers/include/ffx_upscale.h")
    message(FATAL_ERROR "FSR SDK is missing. Run tools/fetch-fsr.ps1 first.")
endif()
add_library(dspaa-graphics STATIC native/graphics/dx11-dx12.cpp)
target_include_directories(dspaa-graphics PUBLIC native)
target_link_libraries(dspaa-graphics PUBLIC d3d11 d3d12 dxgi)
target_compile_definitions(dspaa-graphics PUBLIC NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
target_compile_options(dspaa-graphics PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET dspaa-graphics PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_library(dspaa-fsr STATIC native/fsr/session.cpp)
target_include_directories(dspaa-fsr PUBLIC native PRIVATE "${FSR_ROOT}")
target_link_libraries(dspaa-fsr PUBLIC dspaa-graphics)
target_compile_definitions(dspaa-fsr PUBLIC NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE PRIVATE PLATFORM_WINDOWS)
target_compile_options(dspaa-fsr PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET dspaa-fsr PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_library(dspaa-fg-fsr STATIC native/fsr/presenter.cpp)
target_include_directories(dspaa-fg-fsr PUBLIC native PRIVATE "${FSR_ROOT}")
target_link_libraries(dspaa-fg-fsr PUBLIC dspaa-graphics d3dcompiler)
target_compile_definitions(dspaa-fg-fsr PRIVATE PLATFORM_WINDOWS)
target_compile_options(dspaa-fg-fsr PRIVATE /W4 /permissive- /utf-8 /Zi)
set_property(TARGET dspaa-fg-fsr PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

set(STREAMLINE_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/external/streamline" CACHE PATH "Official NVIDIA Streamline 2.14.1 SDK")
if(NOT EXISTS "${STREAMLINE_ROOT}/include/sl_dlss_g.h")
    message(FATAL_ERROR "Streamline SDK headers are missing.")
endif()
add_library(dspaa-fg-sl STATIC native/sl/runtime.cpp native/sl/security.cpp native/sl/presenter.cpp)
target_include_directories(dspaa-fg-sl PUBLIC native PRIVATE "${STREAMLINE_ROOT}/include")
target_link_libraries(dspaa-fg-sl PUBLIC dspaa-graphics d3dcompiler wintrust crypt32 bcrypt)
target_compile_options(dspaa-fg-sl PRIVATE /W4 /permissive- /utf-8 /Zi)
set_property(TARGET dspaa-fg-sl PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_library(dspaa-present STATIC native/present/native.cpp native/present/latency.cpp native/present/facade.cpp
    native/present/channel.cpp native/present/transport.cpp native/present/sdk-backends.cpp native/present/capture.cpp native/present/capture-trace.cpp native/present/scene-depth.cpp)
target_link_libraries(dspaa-present PUBLIC dspaa-graphics dspaa-fg-fsr dspaa-fg-sl dspaa-capture dspaa-ui-proof user32)
target_compile_options(dspaa-present PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET dspaa-present PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

# The explicit early-load module is separate from ordinary SR-only installation.
add_subdirectory(external/minhook EXCLUDE_FROM_ALL)
set_property(TARGET minhook PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_library(dspaa-capture STATIC native/capture/hooks.cpp native/capture/gpu.cpp native/capture/owner.cpp)
target_link_libraries(dspaa-capture PUBLIC dspaa-graphics minhook d3dcompiler)
target_compile_options(dspaa-capture PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET dspaa-capture PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_library(dspaa-ui-proof STATIC native/ui-proof/guard.cpp native/ui-proof/effects.cpp native/ui-proof/blit.cpp native/ui-proof/shadow.cpp)
target_link_libraries(dspaa-ui-proof PUBLIC dspaa-capture bcrypt)
target_compile_options(dspaa-ui-proof PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET dspaa-ui-proof PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_library(DSPAAPresentBootstrap SHARED native/present/bootstrap.cpp)
target_link_libraries(DSPAAPresentBootstrap PRIVATE bcrypt shell32)
target_compile_definitions(DSPAAPresentBootstrap PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
target_compile_options(DSPAAPresentBootstrap PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET DSPAAPresentBootstrap PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_library(DSPAANative SHARED native/bridge/bridge.cpp native/present/hooks.cpp native/present/interop.cpp)
target_link_libraries(DSPAANative PRIVATE dspaa-core dspaa-ngx dspaa-fsr dspaa-present minhook)
target_compile_definitions(DSPAANative PRIVATE DSPAA_BRIDGE_EXPORTS)
target_compile_options(DSPAANative PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET DSPAANative PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")


add_executable(ngx-probe native/probe/main.cpp)
target_link_libraries(ngx-probe PRIVATE dspaa-core dspaa-ngx DSPAANative dxgi)
target_compile_definitions(ngx-probe PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
target_compile_options(ngx-probe PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET ngx-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_executable(fsr-probe native/probe/fsr.cpp)
target_link_libraries(fsr-probe PRIVATE dspaa-fsr DSPAANative)
target_compile_options(fsr-probe PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET fsr-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_executable(fsr-present-probe native/probe/fsr-present.cpp)
target_link_libraries(fsr-present-probe PRIVATE dspaa-fg-fsr user32 dbghelp)
target_compile_options(fsr-present-probe PRIVATE /W4 /permissive- /utf-8 /Zi)
target_link_options(fsr-present-probe PRIVATE /DEBUG)
set_property(TARGET fsr-present-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_executable(sl-present-probe native/probe/sl-present.cpp)
target_link_libraries(sl-present-probe PRIVATE dspaa-fg-sl user32)
target_compile_options(sl-present-probe PRIVATE /W4 /permissive- /utf-8 /Zi)
target_link_options(sl-present-probe PRIVATE /DEBUG)
set_property(TARGET sl-present-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_executable(present-probe native/probe/present.cpp)
target_link_libraries(present-probe PRIVATE dspaa-present)
target_compile_options(present-probe PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET present-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

# Opt-in, real-adapter behavioral capture proof; never opens a window.
add_executable(ui-capture-probe native/probe/ui-capture.cpp)
target_link_libraries(ui-capture-probe PRIVATE dspaa-ui-proof)
target_compile_options(ui-capture-probe PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET ui-capture-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_executable(scene-depth-probe native/probe/scene-depth.cpp)
target_link_libraries(scene-depth-probe PRIVATE dspaa-present)
target_compile_options(scene-depth-probe PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET scene-depth-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

add_executable(capture-trace-probe native/probe/capture-trace.cpp)
target_link_libraries(capture-trace-probe PRIVATE dspaa-present)
target_compile_options(capture-trace-probe PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET capture-trace-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")

# Explicit, windowless GPU lifecycle proof. Default CTest remains device-independent.
add_executable(constant-shadow-probe native/probe/constant-shadow.cpp)
target_link_libraries(constant-shadow-probe PRIVATE dspaa-ui-proof)
target_compile_options(constant-shadow-probe PRIVATE /W4 /permissive- /utf-8)
set_property(TARGET constant-shadow-probe PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
option(DSPAA_TEST_CONSTANT_SHADOW_GPU "Register the opt-in hardware constant-shadow lifecycle probe" OFF)


option(DSPAA_BUILD_DRIVER_TOOL "Build explicit, manually authorized NVAPI probe-profile tool" OFF)
if(DSPAA_BUILD_DRIVER_TOOL)
    set(NVAPI_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/external/nvapi" CACHE PATH "Official NVAPI SDK")
    add_executable(driver-profile native/tools/driver-profile.cpp)
    target_include_directories(driver-profile SYSTEM PRIVATE "${NVAPI_ROOT}")
    target_link_libraries(driver-profile PRIVATE "${NVAPI_ROOT}/nvapi64.lib")
    target_compile_definitions(driver-profile PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
    target_compile_options(driver-profile PRIVATE /W4 /permissive- /utf-8)
    set_property(TARGET driver-profile PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
endif()


include(CTest)
if(BUILD_TESTING)
    if(DSPAA_TEST_CONSTANT_SHADOW_GPU)
        add_test(NAME constant-shadow-gpu COMMAND constant-shadow-probe)
        set_tests_properties(constant-shadow-gpu PROPERTIES TIMEOUT 60 RUN_SERIAL TRUE LABELS "gpu;opt-in")
    endif()
    add_executable(upload-copy-tests native/tests/upload-copy.cpp)
    target_include_directories(upload-copy-tests PRIVATE native)
    target_compile_definitions(upload-copy-tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN)
    target_compile_options(upload-copy-tests PRIVATE /W4 /permissive- /utf-8)
    set_property(TARGET upload-copy-tests PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
    add_test(NAME upload-copy-bounds COMMAND upload-copy-tests)
    set_tests_properties(upload-copy-bounds PROPERTIES TIMEOUT 10)
    add_executable(bound-uniform-tests native/tests/bound-uniform.cpp)
    target_include_directories(bound-uniform-tests PRIVATE native)
    target_compile_options(bound-uniform-tests PRIVATE /W4 /permissive- /utf-8)
    set_property(TARGET bound-uniform-tests PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
    add_test(NAME bound-uniform COMMAND bound-uniform-tests)
    set_tests_properties(bound-uniform PROPERTIES TIMEOUT 10)
    add_executable(effect-support-tests native/tests/effect-support.cpp)
    target_link_libraries(effect-support-tests PRIVATE dspaa-ui-proof)
    target_compile_options(effect-support-tests PRIVATE /W4 /permissive- /utf-8)
    set_property(TARGET effect-support-tests PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
    add_test(NAME effect-support COMMAND effect-support-tests)
    set_tests_properties(effect-support PROPERTIES TIMEOUT 10)
    add_executable(preset-evidence-tests native/tests/preset-evidence.cpp)
    add_executable(presentation-latency-tests native/tests/presentation-latency.cpp)
    target_link_libraries(presentation-latency-tests PRIVATE dspaa-present)
    target_compile_options(presentation-latency-tests PRIVATE /W4 /permissive- /utf-8)
    set_property(TARGET presentation-latency-tests PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
    add_test(NAME presentation-latency COMMAND presentation-latency-tests)
    set_tests_properties(presentation-latency PROPERTIES TIMEOUT 30)
    add_executable(presentation-channel-tests native/tests/presentation-channel.cpp)
    target_link_libraries(presentation-channel-tests PRIVATE dspaa-present)
    target_compile_options(presentation-channel-tests PRIVATE /W4 /permissive- /utf-8)
    set_property(TARGET presentation-channel-tests PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
    add_test(NAME presentation-channel COMMAND presentation-channel-tests)
    set_tests_properties(presentation-channel PROPERTIES TIMEOUT 10)
    target_link_libraries(preset-evidence-tests PRIVATE dspaa-core)
    set_property(TARGET preset-evidence-tests PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
    add_test(NAME preset-evidence COMMAND preset-evidence-tests)
    add_executable(bridge-tests native/tests/bridge.cpp)
    target_include_directories(bridge-tests PRIVATE native)
    target_link_libraries(bridge-tests PRIVATE DSPAANative d3d11)
    target_compile_definitions(bridge-tests PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
    set_property(TARGET bridge-tests PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
    add_test(NAME bridge-lifetime-fallback COMMAND bridge-tests "${NGX_ROOT}/runtime/rel")
    add_test(NAME bridge-lifetime-fallback-typeless COMMAND bridge-tests "${NGX_ROOT}/runtime/rel" --typeless)
    set_tests_properties(bridge-lifetime-fallback bridge-lifetime-fallback-typeless PROPERTIES TIMEOUT 30)

    add_test(NAME probe-cli COMMAND ${CMAKE_COMMAND} -DPROBE=$<TARGET_FILE:ngx-probe>
        -P ${CMAKE_CURRENT_SOURCE_DIR}/native/tests/probe-cli.cmake)
    set_tests_properties(preset-evidence probe-cli PROPERTIES TIMEOUT 10)
    find_program(POWERSHELL_EXECUTABLE pwsh REQUIRED)
    add_test(NAME dependency-no-clobber COMMAND "${POWERSHELL_EXECUTABLE}" -NoProfile -NonInteractive
        -File "${CMAKE_CURRENT_SOURCE_DIR}/tools/test-download.ps1")
    set_tests_properties(dependency-no-clobber PROPERTIES TIMEOUT 15)
    add_test(NAME managed-policy-interop COMMAND "${POWERSHELL_EXECUTABLE}" -NoProfile -NonInteractive
        -File "${CMAKE_CURRENT_SOURCE_DIR}/tools/test-managed.ps1")
    add_test(NAME game-sandbox-rollback COMMAND "${POWERSHELL_EXECUTABLE}" -NoProfile -NonInteractive
        -File "${CMAKE_CURRENT_SOURCE_DIR}/tools/test-game-sandbox.ps1")
    set_tests_properties(game-sandbox-rollback PROPERTIES TIMEOUT 15)
    set_tests_properties(managed-policy-interop PROPERTIES TIMEOUT 60)
    add_executable(bootstrap-probe native/tests/bootstrap-probe.cpp)
    target_compile_definitions(bootstrap-probe PRIVATE NOMINMAX WIN32_LEAN_AND_MEAN UNICODE _UNICODE)
    add_library(bootstrap-fixture SHARED native/tests/bootstrap-fixture.cpp)
    target_include_directories(bootstrap-fixture PRIVATE native)
    foreach(target bootstrap-probe bootstrap-fixture)
        target_compile_options(${target} PRIVATE /W4 /permissive- /utf-8)
        set_property(TARGET ${target} PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
    endforeach()
    add_test(NAME bootstrap-profile-gate COMMAND "${POWERSHELL_EXECUTABLE}" -NoProfile -NonInteractive
        -File "${CMAKE_CURRENT_SOURCE_DIR}/tools/test-bootstrap.ps1"
        -Bootstrap $<TARGET_FILE:DSPAAPresentBootstrap> -Probe $<TARGET_FILE:bootstrap-probe>
        -Fixture $<TARGET_FILE:bootstrap-fixture> -OutputRoot "${CMAKE_CURRENT_BINARY_DIR}/Testing")
    set_tests_properties(bootstrap-profile-gate PROPERTIES TIMEOUT 30 RUN_SERIAL TRUE)
endif()
